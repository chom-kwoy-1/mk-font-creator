import {TTXObject} from "@/app/TTXObject";
import {Layout} from "@/app/jamo_layouts";

export function generateTtx(ttx: TTXObject, curLayouts: Layout[]) {
    const result = structuredClone(ttx);

    // TODO: Implement this function

    return result;
}
